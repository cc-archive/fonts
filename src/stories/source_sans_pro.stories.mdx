import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { alphabetTable, numberTable, sampleTexts } from './helpers'
import { storyFont } from './story'

<Meta title="Source Sans Pro"  decorators= {[
    storyFn => `<style>
  .source-sans-pro {
    font-family: 'Source Sans Pro';
  }
  .semi-bold {
    font-weight: 600;
  }
  .bold {
    font-weight: 700;
  }
</style>
<div class="source-sans-pro has-text-black has-bottom-margin">
  ${storyFn()}
</div>
<div class="source-sans-pro semi-bold has-text-black has-bottom-margin">
  ${storyFn()}
</div>
<div class="source-sans-pro bold has-text-black">
  ${storyFn()}
</div>`
  ]}/>

<Preview mdxSource = {alphabetTable()}>
  <Story name='uppercaseLetters'>{
    alphabetTable()
  }</Story>
</Preview>

<Preview mdxSource = {alphabetTable(['lowercase'])}>
  <Story name='lowercaseLetters'>{
     alphabetTable(['lowercase'])
  }</Story>
</Preview>

<Preview mdxSource = {numberTable()}>
  <Story name='numbers'>{
    numberTable()
  }</Story>
</Preview>

<Preview mdxSource = {sampleTexts()}>
  <Story name='pangrams'>{
    sampleTexts()
  }</Story>
</Preview>
 
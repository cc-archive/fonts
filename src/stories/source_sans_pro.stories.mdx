import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { alphabetTable, numberTable, sampleTexts } from './helpers'
import { storyFn } from './story'

<Meta title="Source Sans Pro" decorators = {[storyFn]} parameters = {{ alphabetTable, numberTable, sampleTexts }}/>

<Preview>
  <Story name='uppercaseLetters' decorators = {[storyFn]} parameters={{
    design: alphabetTable()
  }}>{
    `<div>
   ${storyFn()}
    <div/>`
  }</Story>
</Preview>

<Preview>
  <Story name='lowercaseLetters' decorators = {[storyFn]} parameters={{
    design: alphabetTable(['lowercase'])
  }}>{
     `<div>
   ${storyFn()}
    <div/>`
  }</Story>
</Preview>

<Preview>
  <Story name='numbers' decorators = {[storyFn]} parameters={{
    design: numberTable
  }}>{
     `<div>
   ${storyFn()}
    <div/>`
  }</Story>
</Preview>

<Preview>
  <Story name='pangrams' decorators = {[storyFn]} parameters={{
    design: sampleTexts
  }}>{
    `<div>
   ${storyFn()}
    <div/>`
  }</Story>
</Preview>
 
